{
  "name": "sveltekit-base",
  "displayName": "SvelteKit",
  "framework": "sveltekit",
  "description": "SvelteKit full-stack framework with TypeScript",
  "files": [
    "src/",
    "static/",
    ".gitignore",
    "package.json",
    "svelte.config.js",
    "tsconfig.json",
    "vite.config.ts"
  ],
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "lint": "eslint src"
  },
  "jsScripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "lint": "eslint src"
  }
}