{
  "name": "components",
  "type": "components",
  "priority": 10,
  "operations": [
    {
      "type": "create-file",
      "source": "shared/shadcn/**",
      "destination": "{{framework == 'nextjs' ? 'components/global/**' : 'src/shared/components/**'}}",
      "condition": { "framework": ["nextjs", "react"], "ui": "shadcn" }
    },
    {
      "type": "create-file",
      "source": "shared/hooks/**",
      "destination": "{{framework == 'nextjs' ? 'hooks/**' : 'src/hooks/**'}}",
      "condition": { "framework": ["nextjs", "react"] }
    },
    {
      "type": "run-command",
      "command": "{{packageManager=='pnpm' ? 'pnpm dlx shadcn@latest add button checkbox field input input-group textarea select badge command popover scroll-area -y' : packageManager=='yarn' ? 'yarn dlx shadcn@latest add button checkbox field input input-group textarea select badge command popover scroll-area -y' : packageManager=='bun' ? 'bun dlx shadcn@latest add button checkbox field input input-group textarea select badge command popover scroll-area -y' : 'npx shadcn@latest add button checkbox field input input-group textarea select badge command popover scroll-area -y'}}",
      "condition": { "framework": ["react", "nextjs"], "ui": "shadcn" }
    },
    {
      "type": "add-dependency",
      "condition": { "framework": ["react", "nextjs"] },
      "dependencies": {
        "@dnd-kit/core": "^6.3.1",
        "@dnd-kit/sortable": "^10.0.0",
        "@dnd-kit/utilities": "^3.2.2"
      }
    }
  ]
}
