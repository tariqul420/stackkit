---
title: Auth.js
description: Add Auth.js (NextAuth.js v5) to your project
---

import { ModuleRequirements, ModuleCreateCommands } from "@/components/docs-meta";

## When to use

Implementing authentication with Auth.js for Next.js + Prisma projects.

## Requirements

<ModuleRequirements moduleType="auth" moduleName="authjs" />

## Installation

### New project

<ModuleCreateCommands moduleType="database" moduleName="prisma" cliPrefix={"npx stackkit@latest create my-app --framework nextjs --auth authjs"} />

### Existing project

```bash
npx stackkit@latest add
# Select "Authentication" → "Auth.js"
```

## What gets generated

- `lib/auth.ts`: Auth configuration
- API routes in `app/api/auth/[...nextauth]/route.ts`
- Prisma models added to schema
- Environment variables

## Setup

1. Set `NEXTAUTH_SECRET` in `.env.local`
2. Set `NEXTAUTH_URL` for production
3. Configure OAuth providers if needed

Next steps:
- [Environment Variables](/docs/reference/environment-variables)
- [Better Auth](/docs/modules/auth/better-auth) for other frameworks