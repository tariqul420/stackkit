---
title: Quick Start
description: Create your first StackKit project in under 5 minutes
---

import { Callout } from 'fumadocs-ui/components/callout';

## Create a New Project

Run the interactive CLI to generate your project:

```bash
npx stackkit create my-app
```

## Choose Your Stack

The CLI will guide you through setup:

```bash
✔ Project name: my-app
✔ Framework: Next.js
✔ Database: Prisma (PostgreSQL)
✔ Auth: Better Auth
✔ Language: TypeScript
✔ Package manager: pnpm

✓ Project created successfully!
```

**Framework Options:**
- **Next.js**: Full-stack React with App Router
- **Express**: Node.js REST API
- **React + Vite**: Frontend-only SPA

**Database Options:**
- **Prisma**: PostgreSQL, MongoDB, MySQL, SQLite
- **Mongoose**: MongoDB only
- **None**: Skip database setup

**Auth Options:**
- **Better Auth**: TypeScript-first, works with all frameworks
- **Auth.js**: Next.js + Prisma only
- **None**: Skip authentication

## Start Development

Navigate to your project and run the dev server:

```bash
cd my-app
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) to see your app.

## Configure Environment Variables

StackKit generates `.env.example` with required variables. Copy it to `.env`:

```bash
cp .env.example .env
```

**For databases:**
```bash
# PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/myapp"

# MongoDB
DATABASE_URL="mongodb://localhost:27017/myapp"
```

**For Better Auth:**
```bash
BETTER_AUTH_SECRET="your-secret-here"  # Generate: openssl rand -base64 32
BETTER_AUTH_URL="http://localhost:3000"
```

<Callout type="warn">
Never commit `.env` files to version control.
</Callout>

## Next Steps

1. **[Set up your database](/docs/modules/database)** - Run migrations and connect
2. **[Configure authentication](/docs/modules/authentication)** - Add OAuth providers
3. **[Check project health](/docs/cli/doctor)** - Verify everything works

## Adding to Existing Projects

Already have a project? Use the modular CLI:

```bash
# Add authentication
npx stackkit add better-auth

# Add database
npx stackkit add prisma --provider postgresql
```

See [CLI Reference](/docs/cli/overview) for all commands.
