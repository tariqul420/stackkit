---
title: list
description: List available frameworks and modules
---

## When to use

Discovering what frameworks and modules are available.

## Command

```bash
npx stackkit@latest list
```

## Options

| Option | Description |
|--------|-------------|
| `-f, --frameworks` | List only frameworks |
| `-m, --modules` | List only modules |

## Examples

```bash
# List everything
npx stackkit@latest list

# List only frameworks
npx stackkit@latest list --frameworks

# List only modules
npx stackkit@latest list --modules
```

## Output

Lists frameworks and modules grouped by category.

Example output:
```
FRAMEWORKS
  ├── Next.js
  ├── Express
  └── React

MODULES
  ├── Auth (1)
  │   └── Better Auth
  ├── Database (2)
  │   ├── Prisma
  │   │   └── Providers: PostgreSQL, Mysql, Sqlite, Mongodb
  │   └── Mongoose
  ├── Ui (1)
  │   └── Shadcn UI
  ├── Storage (1)
  │   └── Cloudinary Storage
  └── Components (1)
      └── Components

Use 'stackkit add <module>' to add modules to your project
```

Next steps:
- [add](/docs/cli/add) to install modules