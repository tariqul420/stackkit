---
title: create-stackkit-app
description: Create new projects with your preferred stack
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';

## Quick Start

Run the interactive CLI to create a new project:

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tab value="pnpm">
    ```bash
    pnpm create stackkit-app
    ```
  </Tab>
  <Tab value="npm">
    ```bash
    npx create-stackkit-app
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn create stackkit-app
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun create stackkit-app
    ```
  </Tab>
</Tabs>

Or specify all options directly:

## Command Syntax

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tab value="pnpm">
    ```bash
    pnpm create stackkit-app [project-name] [options]
    ```
  </Tab>
  <Tab value="npm">
    ```bash
    npx create-stackkit-app [project-name] [options]
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn create stackkit-app [project-name] [options]
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun create stackkit-app [project-name] [options]
    ```
  </Tab>
</Tabs>

## Options

All options are optional. Omit them to use the interactive mode.

### `--framework, -f`

Specify the framework:

```bash
npx create-stackkit-app my-app --framework nextjs
```

**Available frameworks:**
- `nextjs` - Next.js 16 with App Router (Full-stack)
- `express` - Express.js REST API (Backend only)
- `react-vite` - React with Vite (Frontend only)

### `--database, -d`

Specify the database adapter:

```bash
npx create-stackkit-app my-app --database prisma-postgresql
```

**Available databases:**
- `prisma-postgresql` - Prisma ORM with PostgreSQL
- `prisma-mongodb` - Prisma ORM with MongoDB
- `mongoose` - Mongoose ODM with MongoDB
- `skip` - No database

### `--auth, -a`

Specify the authentication provider:

```bash
npx create-stackkit-app my-app --auth better-auth
```

**Available providers:**
- `better-auth` - Better Auth (TypeScript-first, open source)
- `authjs` - Auth.js (NextAuth.js v5, OAuth providers)
- `skip` - No authentication

### `--install`

Install dependencies after project creation (default: `true`).

### `--skip-install`

Skip dependency installation.

### `--git`

Initialize a git repository (default: `true`).

### `--yes, -y`

Use default options without prompts.

### `--help, -h`

Display help information.

## Examples

### Next.js with Prisma and Better Auth

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tab value="pnpm">
    ```bash
    pnpm create stackkit-app my-next-app \
      --framework nextjs \
      --database prisma-postgresql \
      --auth better-auth \
      --install
    ```
  </Tab>
  <Tab value="npm">
    ```bash
    npx create-stackkit-app my-next-app \
      --framework nextjs \
      --database prisma-postgresql \
      --auth better-auth \
      --install
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn create stackkit-app my-next-app \
      --framework nextjs \
      --database prisma-postgresql \
      --auth better-auth \
      --install
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun create stackkit-app my-next-app \
      --framework nextjs \
      --database prisma-postgresql \
      --auth better-auth \
      --install
    ```
  </Tab>
</Tabs>


1. Configure environment variables in `.env.local` or `.env`
2. Run database migrations: `npx prisma migrate dev` (Prisma only)
3. Start development: `npm run dev`

Learn more:
- [Authentication Configuration](/docs/modules/auth)
- [Database Setup](/docs/modules/database)
- [Add Modules to Existing Projects](/docs/cli/stackkit-cli)
